const config = {
  dbName: 'pdf-annotation-db',
  dbVersion: 1,
  outputPaths: {
    sw: '/public/sw.sync.js',
    apiRoutes: '/app/api',
    engine: 'utils/engine.ts',
  },
};

export default config;
